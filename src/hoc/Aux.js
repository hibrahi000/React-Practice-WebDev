const aux = props => props.children;

export default aux;


//this is used so that you do not need to use a div as a root component wraping jsx but react has already created something like this and its called Fragment -->
//Fragment does the same thing you just have to import it with component 